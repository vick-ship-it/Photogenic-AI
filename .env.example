# Replicate API token: https://replicate.com/account/api-tokens
REPLICATE_API_TOKEN=

# Default text-to-image model (conservative default)
REPLICATE_MODEL=black-forest-labs/flux-1.1-pro

# Optional: image-to-image model that accepts an "image" input key
# e.g. fofr/flux-pro-ip-adapter or similar
REPLICATE_IMAGE_TO_IMAGE_MODEL=
